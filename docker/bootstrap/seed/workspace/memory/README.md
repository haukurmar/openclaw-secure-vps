# Memory seed (minimal)

This bootstrap intentionally keeps memory seeding minimal.

- It creates only a neutral folder skeleton.
- No opinionated memory model/governance is enforced here.
- Plug in your next-gen memory system later as Phase 3.

Default OpenClaw memory behavior will still work out of the box.
